[project]
name = "sts2-rl"
version = "0.1.0"
description = "Train a reasoning LLM to play Slay the Spire 2 via RL on Tinker"
requires-python = ">=3.11"
dependencies = [
    "tinker-cookbook",
    "httpx>=0.27",
]

[project.optional-dependencies]
dev = [
    "ruff",
    "pytest",
]
